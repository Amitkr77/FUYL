"use client";
import { ResponsiveContainer, ComposedChart, Line, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from "recharts";
import type { AffiliateAnalytics } from "@/lib/affiliate";
export function AffiliatePerformanceChart({data}:{data:AffiliateAnalytics['series']}){return <div className="h-[320px] w-full"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={data} margin={{top:10,right:20,left:5,bottom:5}}><CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0"/><XAxis dataKey="date" tick={{fontSize:11,fill:'#64748b'}} tickFormatter={v=>new Date(v).toLocaleDateString('en-IN',{day:'2-digit',month:'short'})}/><YAxis yAxisId="money" tick={{fontSize:11,fill:'#64748b'}} tickFormatter={v=>`₹${Number(v)/1000}k`}/><YAxis yAxisId="count" orientation="right" allowDecimals={false} tick={{fontSize:11,fill:'#64748b'}}/><Tooltip formatter={(value:number,name:string)=>[['sales','commission'].includes(name)?`₹${value.toLocaleString('en-IN')}`:value,name.charAt(0).toUpperCase()+name.slice(1)]}/><Legend/><Bar yAxisId="count" dataKey="referrals" fill="#c7d7d3" radius={[3,3,0,0]}/><Line yAxisId="money" dataKey="sales" stroke="#315f52" strokeWidth={2.5} dot={false}/><Line yAxisId="money" dataKey="commission" stroke="#B76E79" strokeWidth={2} dot={false}/></ComposedChart></ResponsiveContainer></div>}
