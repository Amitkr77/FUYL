"use client";
import { useState } from "react";
import type { Affiliate } from "@/lib/affiliate";
import { saveAffiliateReviewAction } from "@/app/(admin)/affiliates/actions";
export function AffiliateReviewCard({affiliate}:{affiliate:Affiliate}){const[saving,setSaving]=useState(false),[message,setMessage]=useState("");async function submit(data:FormData){setSaving(true);const r=await saveAffiliateReviewAction(affiliate.id,{internalNote:String(data.get('internalNote')||''),fraudStatus:String(data.get('fraudStatus')) as 'clear'|'review'|'blocked',fraudNote:String(data.get('fraudNote')||'')});setSaving(false);setMessage(r.error??'Review saved.')}return <section className="rounded-xl border bg-white p-5"><h3 className="font-semibold">Internal review</h3><form action={submit} className="mt-4 space-y-3"><label className="block text-xs font-medium text-slate-500">Fraud status<select name="fraudStatus" defaultValue={affiliate.metadata?.fraudReview?.status??'clear'} className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"><option value="clear">Clear</option><option value="review">Needs review</option><option value="blocked">Blocked</option></select></label><label className="block text-xs font-medium text-slate-500">Fraud review note<textarea name="fraudNote" defaultValue={affiliate.metadata?.fraudReview?.note} rows={2} className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"/></label><label className="block text-xs font-medium text-slate-500">Internal note<textarea name="internalNote" defaultValue={affiliate.metadata?.internalNote} rows={3} className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"/></label>{message&&<p className="text-xs text-slate-500">{message}</p>}<button disabled={saving} className="rounded-lg bg-[#12291F] px-3 py-2 text-sm font-semibold text-white disabled:opacity-50">{saving?'Saving…':'Save review'}</button></form></section>}
