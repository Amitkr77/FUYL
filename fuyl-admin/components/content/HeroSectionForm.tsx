"use client";
import { useState } from "react";
import { Plus,Trash2 } from "lucide-react";
import type { HeroSection } from "@/lib/content";
import { updateHeroAction } from "@/app/(admin)/content/actions";
const blank=()=>({id:crypto.randomUUID(),eyebrow:'',headline:'',subheading:'',image:'',imageAlt:'',primaryCtaLabel:'Shop now',primaryCtaHref:'/products',secondaryCtaLabel:'',secondaryCtaHref:''});
export function HeroSectionForm({initial}:{initial:HeroSection}){const[hero,setHero]=useState(initial),[saving,setSaving]=useState(false),[message,setMessage]=useState('');const field='mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm';function patch(i:number,key:string,value:string){setHero({...hero,data:{...hero.data,slides:hero.data.slides.map((s,n)=>n===i?{...s,[key]:value}:s)}})}async function save(){setSaving(true);const r=await updateHeroAction(hero);setSaving(false);setMessage(r.error??'Hero section saved.')}return <div className="space-y-5"><section className="rounded-xl border bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-semibold">Hero settings</h3><p className="text-sm text-slate-500">Control visibility and slide timing.</p></div><label className="flex gap-2 text-sm"><input type="checkbox" checked={hero.isActive} onChange={e=>setHero({...hero,isActive:e.target.checked})}/>Visible on storefront</label></div><label className="mt-4 block max-w-xs text-sm font-medium">Autoplay duration (milliseconds)<input type="number" min="2000" value={hero.data.autoplayMs} onChange={e=>setHero({...hero,data:{...hero.data,autoplayMs:Number(e.target.value)}})} className={field}/></label></section>{hero.data.slides.map((s,i)=><section key={s.id} className="rounded-xl border bg-white p-5"><div className="flex justify-between"><h3 className="font-semibold">Slide {i+1}</h3><button onClick={()=>setHero({...hero,data:{...hero.data,slides:hero.data.slides.filter((_,n)=>n!==i)}})} className="text-red-500"><Trash2 className="h-4 w-4"/></button></div><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Eyebrow<input value={s.eyebrow} onChange={e=>patch(i,'eyebrow',e.target.value)} className={field}/></label><label className="text-sm font-medium">Image path or URL<input value={s.image} onChange={e=>patch(i,'image',e.target.value)} className={field}/></label><label className="text-sm font-medium sm:col-span-2">Heading <span className="font-normal text-slate-400">(new line creates a line break)</span><textarea rows={3} value={s.headline} onChange={e=>patch(i,'headline',e.target.value)} className={field}/></label><label className="text-sm font-medium sm:col-span-2">Subheading<textarea rows={2} value={s.subheading} onChange={e=>patch(i,'subheading',e.target.value)} className={field}/></label><label className="text-sm font-medium">Image alt text<input value={s.imageAlt} onChange={e=>patch(i,'imageAlt',e.target.value)} className={field}/></label><div/><label className="text-sm font-medium">Primary button label<input value={s.primaryCtaLabel} onChange={e=>patch(i,'primaryCtaLabel',e.target.value)} className={field}/></label><label className="text-sm font-medium">Primary button link<input value={s.primaryCtaHref} onChange={e=>patch(i,'primaryCtaHref',e.target.value)} className={field}/></label><label className="text-sm font-medium">Secondary button label<input value={s.secondaryCtaLabel} onChange={e=>patch(i,'secondaryCtaLabel',e.target.value)} className={field}/></label><label className="text-sm font-medium">Secondary button link<input value={s.secondaryCtaHref} onChange={e=>patch(i,'secondaryCtaHref',e.target.value)} className={field}/></label></div></section>)}<div className="flex flex-wrap justify-between gap-3"><button onClick={()=>setHero({...hero,data:{...hero.data,slides:[...hero.data.slides,blank()]}})} className="inline-flex items-center gap-2 rounded-lg border px-4 py-2 text-sm"><Plus className="h-4 w-4"/>Add slide</button><div className="text-right">{message&&<p className="mb-1 text-sm text-slate-500">{message}</p>}<button disabled={saving||!hero.data.slides.length} onClick={save} className="rounded-lg bg-[#12291F] px-5 py-2 text-sm font-semibold text-white disabled:opacity-50">{saving?'Saving…':'Save hero'}</button></div></div></div>}
