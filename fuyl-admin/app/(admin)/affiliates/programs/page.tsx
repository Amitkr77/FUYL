import Link from "next/link";
import { Plus } from "lucide-react";
import { listAffiliatePrograms } from "@/lib/affiliate";
import { ProgramActions } from "@/components/affiliates/ProgramActions";
export default async function Page() {
  const programs = await listAffiliatePrograms();
  return <div className="space-y-5"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold">Affiliate programs</h2><p className="text-sm text-slate-500">Define commissions and attribution for affiliate groups.</p></div><Link href="/affiliates/programs/new" className="inline-flex items-center gap-2 rounded-lg bg-[#12291F] px-4 py-2 text-sm font-semibold text-white"><Plus className="h-4 w-4"/>Create program</Link></div><div className="overflow-x-auto rounded-xl border border-slate-100 bg-white"><table className="w-full text-sm"><thead><tr className="border-b bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><th className="px-5 py-3">Program</th><th className="px-5 py-3">Commission</th><th className="px-5 py-3">Affiliates</th><th className="px-5 py-3">Attribution</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Actions</th></tr></thead><tbody>{programs.map(p=><tr key={p._id} className="border-b last:border-0"><td className="px-5 py-4"><div className="flex items-center gap-2"><span className="font-semibold">{p.name}</span>{p.isDefault&&<span className="rounded-full bg-amber-50 px-2 py-0.5 text-[11px] font-medium text-amber-700">Default</span>}</div><p className="mt-1 max-w-sm truncate text-xs text-slate-400">{p.description||"No description"}</p></td><td className="px-5 py-4"><span className="font-semibold">{p.defaultRate}%</span><p className="text-xs text-slate-400">of {p.commissionBase.replace("_"," ")}</p></td><td className="px-5 py-4">{p.affiliateCount??0}</td><td className="px-5 py-4">{p.attributionWindowDays} days</td><td className="px-5 py-4"><span className={`rounded-full px-2 py-1 text-xs ${p.isActive?"bg-green-50 text-green-700":"bg-slate-100 text-slate-500"}`}>{p.isActive?"Active":"Inactive"}</span></td><td className="px-5 py-4"><ProgramActions id={p._id} isDefault={p.isDefault}/></td></tr>)}</tbody></table>{!programs.length&&<p className="p-12 text-center text-sm text-slate-400">No programs yet.</p>}</div></div>;
}
